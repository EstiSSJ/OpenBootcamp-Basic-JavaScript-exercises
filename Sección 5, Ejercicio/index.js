const alturaCm = 170;
const alturaM = alturaCm / 100;

const pesoKg = 69.4;

const alturaMArriba = Math.ceil(alturaM);
const pesoKgAbajo = Math.floor(pesoKg);

const maxValorJS = Number.MAX_VALUE;
const esIgual = maxValorJS + 1 === maxValorJS;
